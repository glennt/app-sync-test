import Baobab from 'baobab';
import _ from 'lodash';

import defaultState from './defaultState';

const baobabConfig = {
};

const tree = new Baobab(defaultState, baobabConfig);

export default tree;
